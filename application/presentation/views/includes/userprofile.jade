.profile.border.rounded-3.py-3 
    -if(user){
        .row
            .col-md-12.text-center 
                h4 #{user.firstname} #{user.lastname}
                p #{user.nickname}
                h4 Friends 
                p 15
                p
                    a(href="/messenger/#{user.nickname}/chat")  
                        img(src="/images/mail-inbox.png" width="30px")
                        br
                        | Enviar mensaje 
                        
    -} else{
        .row
            .col-md-12.text-center
                b No se pudo obtener el perfil 
    -}