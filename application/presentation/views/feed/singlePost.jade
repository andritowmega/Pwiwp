extends ../layout

block content
  .row.mt-5 
    .col-md-3 
        include ../includes/profile
    .col-md-9.text-center 
        .row 
            .col#post
                .row.mt-2
                    .col-md-12.border.rounded-3.placeholder-glow 
                        -if(post){
                            .row 
                                .col-4 
                                    b 
                                        | Autor: 
                                        a(href="/profile/#{post.nickname}") #{post.firstname} #{post.lastname}
                                .col-9 
                            hr
                            .row 
                                .col-12 #{post.content}
                            .row 
                                .col-12.text-left#reaction 
                                    span#countReaction -
                                    a(href="javascript:reaction(#{post.id})")
                                        img(src="/images/like.png" width="20px") 
                                        |  Dar Piw
                        -}else{
                            b No se pudo obtener la publicación
                        -}
                        
                                
        include ../includes/comments
    script.
      window.addEventListener('load', function() {
        getComments(#{post.id});
        getReactions(#{post.id});
      });
        
